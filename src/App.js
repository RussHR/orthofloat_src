import React, { PropTypes } from 'react';

const App = () => {
  return (
    <div>
      welp I am the app
    </div>
  );
};

App.propTypes = {
  children: PropTypes.element
};

export default App;
